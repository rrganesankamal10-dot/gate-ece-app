name: gate_ece_app
description: GATE ECE study companion — formulas, quizzes, circuit diagrams.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  # google_mobile_ads: ^5.1.0   # uncomment once we get to the ads step
  # cloud_firestore: ^5.0.0    # uncomment once we connect Firebase for content

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
